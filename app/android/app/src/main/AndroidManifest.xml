<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter ne déclare INTERNET que pour debug et profile : sans cette
         ligne, la version release ne peut joindre ni Supabase ni Scryfall. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Saisie vocale. RECORD_AUDIO est demandée à l'exécution ;
         la requête <queries> est indispensable sur Android 11+ pour que
         l'application puisse seulement VOIR le moteur de reconnaissance —
         sans elle, speech_to_text rapporte « indisponible » sur un appareil
         parfaitement capable. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Flux caméra du mode temps réel. Elle était jusqu'ici inutile : la
         saisie photo passe par l'application appareil photo du système, qui
         rend une image sans que DeckHand n'ait accès au capteur. Lire le flux
         en continu, si. Déclarée ici plutôt que laissée à la fusion du
         manifeste du greffon, pour que ce que l'application demande se lise
         dans l'application. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="DeckHand"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Recadrage de la photo avant reconnaissance. image_cropper
             s'appuie sur uCrop, dont l'activité doit être déclarée ici :
             sans cela l'application plante à l'ouverture du recadrage,
             et seulement en conditions réelles. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
