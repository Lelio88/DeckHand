"""Mesures produit — ce que l'usage réel coûte, en temps et en gestes.

Distinct de `ingestion/` (alimenter la base) et de `vision/` (construire
l'index) : ici on ne fabrique rien, on observe. Ces scripts servent à trancher
des questions de conception sur des chiffres plutôt que sur des impressions.
"""
